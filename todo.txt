yaron
##############
    production
    ###########
    change mail of admin


with mor
###################
tags - smaller again
insert newsletter
font on prices (h3?)
decide title of tabs


before sync
################
two css's
class MenuItem(OrderedModel):

    link_url = models.CharField(max_length=100, help_text='URL or URI to the content, eg /about/ or http://foo.com/')
    title = models.CharField(max_length=100)                             

    def __unicode__(self):
        return "%s. %s" % (self.order, self.title)